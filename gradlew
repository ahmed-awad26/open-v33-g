#!/usr/bin/env sh
# Placeholder wrapper script.
# Run `gradle wrapper` locally or in a CI preparation step to generate the full Gradle wrapper files.
# This starter intentionally omits downloaded wrapper binaries.
exec gradle "$@"
