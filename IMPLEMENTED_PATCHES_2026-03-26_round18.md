# Round 18 changes

- Removed the inline top progress surface from Import & Export.
- Normalized transfer action card heights and clamped text to keep side-by-side tiles visually equal.
- Added a main transfer path editor in Import & Export, backed by the existing export path setting.
- Updated import staging copies to respect the current main transfer root under Downloads/AW.
- Converted the floating progress overlay into a compact bottom card without dimming the full screen.

- Narrowed the import/export and backup floating progress UI so it no longer uses a full-screen transparent overlay. The floating card now only occupies the active panel area at the bottom.
