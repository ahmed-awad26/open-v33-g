# Round 20

- Fixed folder cover rendering in the drawer carousel by making the image loader support both absolute file paths and content URIs.
- Adjusted the folder carousel tile so the saved folder image can visually fill the tile without the top folder icon blocking it when an image exists.
- Preserved the current rounded corners and full-tile crop behavior.
